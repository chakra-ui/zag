---
"@zag-js/toast": patch
---

Expose viewport offset as CSS variables (`--viewport-offset-left`, `--viewport-offset-right`, `--viewport-offset-top`,
`--viewport-offset-bottom`) on the toast group element.
