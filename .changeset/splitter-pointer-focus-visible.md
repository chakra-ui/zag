---
"@zag-js/splitter": patch
---

Prevent pointer interactions with elements returned by `getResizeTriggerProps()` from matching `:focus-visible` while
preserving focus for subsequent keyboard resizing.
