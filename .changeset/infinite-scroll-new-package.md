---
"@zag-js/infinite-scroll": minor
---

Add `@zag-js/infinite-scroll`, a machine for loading paginated content as the user scrolls. Supports both scroll edges
(`end` for feeds, `start` for chat threads with scroll anchoring), horizontal and RTL layouts, controlled and
promise-derived loading, and screen reader announcements for loaded and completed states.
