---
"@zag-js/avatar": minor
---

Expose the image loading status. Adds `api.status` (`loading | loaded | error`) and emits `loading` from `onStatusChange`, so you can render a distinct loading state.
