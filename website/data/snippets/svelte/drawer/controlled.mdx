```svelte
<script lang="ts">
  let open = $state(false)

  const service = useMachine(drawer.machine, {
    get open() {
      return open
    },
    onOpenChange(details) {
      open = details.open
    },
  })
</script>
```
