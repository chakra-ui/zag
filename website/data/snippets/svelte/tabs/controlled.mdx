```svelte
<script lang="ts">
  let value = $state("tab-1")

  const service = useMachine(tabs.machine, {
    get value() {
      return value
    },
    onValueChange(details) {
      value = details.value
    },
  })
</script>
```
