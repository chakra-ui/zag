```svelte
<script lang="ts">
  let open = $state(false)

  const service = useMachine(bottomSheet.machine, {
    get open() {
      return open
    },
    onOpenChange(details) {
      open = details.open
    },
  })
</script>
```
