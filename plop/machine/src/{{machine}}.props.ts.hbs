import { createProps } from "@zag-js/types"
import { createSplitProps } from "@zag-js/utils"
import type { {{pascalCase machine}}Props } from "./{{dashCase machine}}.types"

export const props = createProps<{{pascalCase machine}}Props>()(["dir", "id", "getRootNode"])
export const splitProps = createSplitProps<Partial<{{pascalCase machine}}Props>>(props)
